# Database Configuration
DATABASE_URL=postgresql://refract:refract_dev@postgres:5432/refract_iot

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Configuration
# Comma-separated list of allowed origins (e.g., "https://example.com,https://app.example.com")
# Leave empty to use default localhost origins for development
CORS_ORIGINS=

# API Key Authentication
# Set to "true" to require API key authentication
API_KEY_REQUIRED=false
# API key for authentication (generate a strong random key for production)
API_KEY=your-secret-api-key-here

# Environment
ENVIRONMENT=development
